// ── Shared mutable state ──────────────────────────────────
// Single object — mutations are visible across all modules.
export const state = {
  currentUser:    null,
  currentProfile: null,
  currentGig:     null,
  sellerPayDetails: null,
};
